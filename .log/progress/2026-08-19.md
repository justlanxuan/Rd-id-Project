# Progress：2026-08-19

- 根据用户确认，将 E1 门禁改为 per-source selective gate：`included`、`conditional`、`excluded`、`pending`，不要求所有骨架源通过。
- A2 语义审计完成：TotalCapture/EgoHumans canonical 样本 person/IMU 显式相等；Custom 四 fold 的 12 个 window CSV、7380 行和 7380 个 NPZ 引用全部一致；S06 train/val 共 108 个 baseline 映射全部一致。
- 当前最小可信子集为 TotalCapture GT、EgoHumans canonical、Custom canonical、S06 AlphaPose、FMPose3D、MotionAGFormer、TCPFormer、WHAM；YOLO-Pose high 为 conditional，raw cache 为 pending。
- A3 已生成 `/data/fzliang/reid-project/g9/e1_gap_audit/gap_profile.json`，记录输入审计 hash、坐标空间/时间身份/质量证据和四个待检验 gap 假设。
- A2 进一步确认 `[T,P,17,3]` 不等于 3D：EgoHumans/AlphaPose/YOLO-Pose 是 2D xy+visibility/zero-z；TotalCapture 和四个 3D lifter/WHAM 才是 xyz。
- B1 全量 E2/E4 screening 完成，生成 `/data/fzliang/reid-project/g9/e2_multimodal/multimodal_motion_diagnostics.json`：IMU schema、raw 10 Hz→Custom 约 30 fps 重采样、运动复杂度 tertiles 和 lag correlation。
- B2 生成 `/data/fzliang/reid-project/g9/e2_multimodal/tracking_quality.json`：S06 visibility/tracklet 分层完成，独立 ID switch 因没有独立 track IDs 不能识别。
- C1 生成 `/data/fzliang/reid-project/g9/e2_multimodal/imu_contract_comparison.json`：legacy48 已显式转换为 L_LowArm 7D；Custom 发现 1612/177120（0.91%）帧 quaternion norm 无效，其中 28 帧为零四元数。
- A4 生成 `/data/fzliang/reid-project/g9/e1_gap_audit/coordinate_outlier_audit.json`：EgoHumans raw xy 的 94.9% 坐标值 `abs>10`；YOLO-Pose high 有 996 个 `abs>10` 值分布于 54/88 序列，并已记录具体 frame/person/joint 极值；同表示归一化 pairwise 已完成。
- E3 D1 索引现有 G6 66 个 evaluation records（22 cells），并显式列出 S06 source sweep、representation/IMU controls 和 prediction-level 分层的缺失条件。
- E7 已生成 `/data/fzliang/reid-project/g9/g9_final_gap_manifest.json`，状态为 `diagnostic_complete_causal_controls_pending`；所有 E1/E2/E3 输入均带 SHA256，且 causal claim 明确为 false。
- D2 已对现有 G6 的 105 个 session clips 重算 history/instantaneous `correct/total`，按 motion tertile、candidate group size 和 visible people 分层，0 个 segment 缺失；新 S06 source sweep 仍待执行。
- Ruff、py_compile、git diff --check 通过；提交 `5b52dee`、`de95160`。

## Next

1. 在可信子集上做 root/torso normalization 对照并审计 EgoHumans 坐标空间；
2. 对 YOLO-Pose high 做逐帧逐关节 outlier/coverage 检查；
3. 统一 canonical/S06 IMU 7D contract 后重跑 lag/复杂度；
4. 冻结 G9 protocol 后开始带预测 `correct/total` 的 source/target sweep。
